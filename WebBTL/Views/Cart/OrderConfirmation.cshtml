@model WebBTL.Models.Order

@{
    ViewBag.Title = "Order Confirmation";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<p>Cảm ơn bạn đã đặt hàng! Đơn hàng của bạn đã được ghi nhận.</p>

<h4>Thông tin đơn hàng:</h4>
<p><strong>Mã đơn hàng:</strong> @Model.OrderID</p>
<p><strong>Ngày đặt hàng:</strong> @Model.Orderdate?.ToString("dd/MM/yyyy")</p>
<p><strong>Tổng số tiền:</strong> @String.Format("{0:C}", @Model.TotalAmount)</p>

<h4>Chi tiết đơn hàng:</h4>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Sản phẩm</th>
            <th>Số lượng</th>
            <th>Đơn giá</th>
            <th>Thành tiền</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var detail in Model.OrderDetails)
        {
            <tr>
                <td>@detail.Product.ProductName</td>
                <td>@detail.Quantity</td>
                <td>@String.Format("{0:C}", detail.Price)</td>
                <td>@String.Format("{0:C}", detail.Price * detail.Quantity)</td>
            </tr>
        }
    </tbody>
</table>

<p>Bạn có thể xem lại đơn hàng của mình trong mục quản lý đơn hàng cá nhân.</p>